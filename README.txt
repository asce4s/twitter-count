Application was created using Nodejs 8.4

In order to run the application first install nodejs
https://nodejs.org/en/

On the terminal change directory to the project folder and run the following commands
npm install
npm start

application will start on localhost:3000

